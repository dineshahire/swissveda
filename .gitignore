node_modules
dist
.DS_Store
*.log

# large/local media — exceed GitHub's 100MB/file limit, not needed in repo
_backup/
*.mp4
*.zip
*.webm

# …except the web-optimized scroll videos that the site actually serves
!public/videos/
!public/videos/*.mp4
